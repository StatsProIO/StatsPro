on:
  push:
    branches: [ master ]
name: Build Staging
jobs:
  deploy-staging:
    name: "Deploy Staging"
    uses: ./.github/workflows/build.yml
    with:
      environment: staging
    secrets: inherit