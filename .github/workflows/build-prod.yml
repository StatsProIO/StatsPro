on:
  push:
    branches: [ prod ]
  workflow_dispatch:
name: Build Prod

jobs:
  deploy-prod:
    name: "Deploy Prod"
    uses: ./.github/workflows/build.yml
    with:
      environment: prod
    secrets: inherit