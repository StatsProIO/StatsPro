import {Typography} from '@mui/material';
import React from 'react';


export default function NoData( ) {

        return <Typography variant="body1" align={'center'} color={'text.secondary'} sx={{my: 3}}>No data</Typography>
}
